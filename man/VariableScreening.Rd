% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/VariableScreening.R
\name{VariableScreening}
\alias{VariableScreening}
\title{Variables Screening Methods Based on Prior Knowledge and Marginal Utility}
\usage{
VariableScreening(
  X,
  Y,
  delta,
  disease_genes,
  screening = NULL,
  model = NULL,
  dist = NULL,
  rank_method = NULL,
  d = NULL,
  standardize = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{X}{Numeric matrix of covariates.}

\item{Y}{Numeric vector of observed survival times (log-transformed under \code{AFTNet}).}

\item{delta}{Integer vector of censoring indicators (1 = event, 0 = censored).}

\item{disease_genes}{Character vector containing the names of genes known
to be associated with diseases.}

\item{screening}{Character string specifying the screening method
(\code{"BMD"}, \code{"DAD"}, or \code{"BMD+DAD"}).}

\item{model}{Character string specifying the fitted survival model
(\code{"COXNet"}, or \code{"AFTNet"}) required for DAD-based screening.}

\item{dist}{Character string specifying the AFTNet distribution.
Must be one of \code{"weibull"}, \code{"lognormal"}, or \code{"loglogistic"}.}

\item{rank_method}{Character string specifying the ranking criterion for DAD-based screening:
\code{"absmg"} (absolute marginal coefficients),
\code{"mg"} (marginal function), or \code{"mgpadj"}
(adjusted p-value from the marginal function).}

\item{d}{Numeric value representing the threshold for top-ranked features to select
in DAD-based screening (\code{default: NULL}).}

\item{standardize}{Logical value indicating whether to standardize the input matrix
in DAD-based screening:
\itemize{
\item if \code{TRUE} (default) each column is centered to have
mean 0 and scaled to have unit variance.
\item if \code{FALSE} the function assumes that the matrix has
already been standardized by the user.
}}

\item{verbose}{Logical value, if \code{TRUE} progress messages are printed (\code{default: FALSE}).}
}
\value{
A list containing selected variable names \code{screen_vars}.
}
\description{
Reduces the high-dimensional feature space to a more manageable
subset of variables by applying one of three screening strategies:
\itemize{
\item \strong{BMD (Biomedical-driven)}: selects covariates based on prior
biomedical knowledge about their relevance to the disease under investigation,
\item \strong{DAD (Data-driven)}: selects features using component-wise
estimators obtained from the chosen penalized model,
\item \strong{BMD+DAD}: combines both biomedical knowledge and data-driven insights.
}
}
\details{
The function uses marginal ranking approaches to select features based on
their association with survival outcomes.
\itemize{
\item In the \strong{BMD} approach, prior knowledge comes from literature or
external biological databases such as \href{https://hb.flatironinstitute.org/}{HumanBase}.
\item The \strong{DAD} screening computes marginal regression coefficients to
rank features according to their estimated importance
under the selected model:
\itemize{
\item \code{absmg}: top \code{d} covariates by largest
absolute marginal coefficients.
\item \code{mg}: top \code{d} covariates by largest marginal
coefficients, preserving the direction.
\item \code{mgpadj}: top \code{d} covariates passing significance
thresholds based on adjusted p-values.}
\item The \strong{BMD+DAD} combines prior biological knowledge and data-driven
selection for comprehensive feature screening.
}
}
\seealso{
\code{\link{CreateNetwork}} or \code{\link{RepositoryDisease}} for the \code{disease_genes} names.
}
